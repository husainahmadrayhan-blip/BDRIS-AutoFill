# Production Login Fix

This build restores the production User Login / Access Link flow.
The LOCAL DIRECT mode was removed from the main application UI.
Main application API routes require a valid authenticated user session.
Use the access link generated by Admin, then sign in with the user credentials.
